import React from 'react';
import InputField from '../InputField/InputField';


// table data will take in the input from InputField
// the checkerboard will resize with the data from InputField

let TableData = (props) => {
    return (
        <div>
            <InputField />
        </div>
    )
}

export default TableData;